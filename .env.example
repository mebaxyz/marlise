# Example environment variables for local development
# Copy this file to .env and adjust values as needed.

# Client interface (FastAPI) port
CLIENT_INTERFACE_PORT=8080

# Session Manager ZeroMQ ports (these are the defaults used in the repo)
# - RPC (REQ/REP)
SESSION_MANAGER_RPC_PORT=5718
# - PUB (publisher)
SESSION_MANAGER_PUB_PORT=6718
# - SUB (subscriber)
SESSION_MANAGER_SUB_PORT=7718

# Modhost-bridge ZeroMQ endpoints (bridge ↔ session manager)
MODHOST_BRIDGE_REP=tcp://127.0.0.1:6000
MODHOST_BRIDGE_PUB=tcp://127.0.0.1:6001
MODHOST_BRIDGE_HEALTH=tcp://127.0.0.1:6002

# mod-host TCP command/feedback ports
MOD_HOST_CMD=127.0.0.1:5555
MOD_HOST_FEEDBACK=127.0.0.1:5556

# LV2 plugin directory used by the audio host
LV2_PATH=/usr/lib/lv2

# Development mode
NODE_ENV=development

# Notes:
# - This file is intended as an example. Services may read different or
#   additional environment variables; consult component READMEs for more.